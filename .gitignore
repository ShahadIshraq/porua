# Rust build artifacts
target/
Cargo.lock

# Generated audio files (uncomment if you don't want to track them)
# *.wav

# macOS system files
.DS_Store

# Model files (usually large and shouldn't be in git)
*.onnx
*.bin
server/models/

# IDE
.vscode/
.idea/

# Test coverage
plugin/coverage/

# Plugin build artifacts (also in plugin/.gitignore but added here for safety)
plugin/dist/
plugin/packages/
plugin/.packaging-temp/
plugin/*.zip
plugin/*.xpi

# Plan files (should not be committed)
*_PLAN.md
*_SUMMARY.md
