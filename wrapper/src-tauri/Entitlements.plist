<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow DYLD environment variables (for passing env vars to server binary) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Allow unsigned executable memory (may be needed for ONNX Runtime) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow JIT compilation (for ONNX Runtime optimizations) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Disable library validation to allow loading dynamic libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Screen Recording permission (for overlay feature) -->
    <key>com.apple.security.device.camera</key>
    <false/>
</dict>
</plist>
